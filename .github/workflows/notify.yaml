name: Notify

on:
  workflow_run:
    workflows:
      - Publish
    types:
      - completed

jobs:
  run:
    name: Notify
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: master

      - name: Notify
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.PAT_TOKEN }}
          script: |
            const { version } = JSON.parse(require('fs').readFileSync('yarn/cli/package.json', 'utf8'))

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'common-modules',
              workflow_id: 'tools.yaml',
              ref: 'master',
              inputs: {
                version,
              }
            })

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'telemetry-modules',
              workflow_id: 'tools.yaml',
              ref: 'master',
              inputs: {
                version,
              }
            })

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'mikro-orm-modules',
              workflow_id: 'tools.yaml',
              ref: 'master',
              inputs: {
                version,
              }
            })

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'nestjs-modules',
              workflow_id: 'tools.yaml',
              ref: 'master',
              inputs: {
                version,
              }
            })

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'react-modules',
              workflow_id: 'tools.yaml',
              ref: 'master',
              inputs: {
                version,
              }
            })

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'buildpacks',
              workflow_id: 'tools.yaml',
              ref: 'master',
              inputs: {
                version,
              }
            })

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'types',
              workflow_id: 'tools.yaml',
              ref: 'master',
              inputs: {
                version,
              }
            })

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: 'ui',
              workflow_id: 'tools.yaml',
              ref: 'master',
              inputs: {
                version,
              }
            })
